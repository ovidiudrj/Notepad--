<Window x:Class="Tema_MAP.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:Tema_MAP.ViewModels"
        Title="Notepad++ Clone" Height="600" Width="900">
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Menu Grid.Row="0">
            <MenuItem Header="File">
                <MenuItem Header="Open Folder" Command="{Binding OpenWorkspaceCommand}"/>
                <MenuItem Header="New file" Command="{Binding NewFileCommand}"/>
                <MenuItem Header="Open file" Command="{Binding OpenFileCommand}"/>
                <MenuItem Header="Save file" Command="{Binding SaveFileCommand}"/>
                <MenuItem Header="Save file as" Command="{Binding SaveFileAsCommand}"/>
                <MenuItem Header="Close current file" Command="{Binding CloseFileCommand}"/>
                <Separator/>
                <MenuItem Header="Close all files" Command="{Binding CloseAllFilesCommand}"/>

            </MenuItem>

            <MenuItem Header="Search">
                <MenuItem Header="Find" Command="{Binding OpenSearchCommand}"/>
            </MenuItem>

            <MenuItem Header="View">
                <MenuItem Header="Standard" Command="{Binding ViewStandardCommand}"/>
                <MenuItem Header="Folder Explorer" Command="{Binding ViewFolderExplorerCommand}"/>
            </MenuItem>
            <MenuItem Header="Help">
                <MenuItem Header="About" Command="{Binding AboutCommand}"/>
            </MenuItem>
        </Menu>

        <ToolBar Grid.Row="1">
            <Button Content="New" Command="{Binding NewFileCommand}"/>
            <Button Content="Open" Command="{Binding OpenFileCommand}"/>
            <Button Content="Save" Command="{Binding SaveFileCommand}"/>
            <Button Content="Save As" Command="{Binding SaveFileAsCommand}"/>
            <Separator/>
            <Button Content="Close All" Command="{Binding CloseAllFilesCommand}"/>
        </ToolBar>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TreeView Grid.Column="0" Name="FolderTreeView" Width="250" 
                      ItemsSource="{Binding Workspace}"
                      Visibility="{Binding FolderExplorerVisibility}">

                <TreeView.ItemContainerStyle>
                    <Style TargetType="TreeViewItem">
                        <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}" />
                        <Setter Property="Tag" Value="{Binding DataContext, RelativeSource={RelativeSource AncestorType=TreeView}}" />

                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsFile}" Value="False">
                                <Setter Property="ContextMenu">
                                    <Setter.Value>
                                        <ContextMenu>
                                            <MenuItem Header="New file" Command="{Binding PlacementTarget.Tag.NewFileInTreeCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}" CommandParameter="{Binding}" />
                                            <MenuItem Header="Copy path" Command="{Binding PlacementTarget.Tag.CopyPathCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}" CommandParameter="{Binding}" />
                                            <MenuItem Header="Copy folder" Command="{Binding PlacementTarget.Tag.CopyFolderCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}" CommandParameter="{Binding}" />
                                            <MenuItem Header="Paste folder" Command="{Binding PlacementTarget.Tag.PasteFolderCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}" CommandParameter="{Binding}" />
                                        </ContextMenu>
                                    </Setter.Value>
                                </Setter>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TreeView.ItemContainerStyle>

                <TreeView.ItemTemplate>
                    <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                        <TextBlock Text="{Binding Name}">
                            <TextBlock.InputBindings>
                                <MouseBinding MouseAction="LeftDoubleClick" 
                                              Command="{Binding DataContext.OpenTreeFileCommand, RelativeSource={RelativeSource AncestorType=TreeView}}" 
                                              CommandParameter="{Binding FullPath}"/>
                            </TextBlock.InputBindings>
                        </TextBlock>
                    </HierarchicalDataTemplate>
                </TreeView.ItemTemplate>
            </TreeView>

            <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" Background="LightGray"
                          Visibility="{Binding FolderExplorerVisibility}"/>

            <TabControl Grid.Column="2" Name="FilesTabControl" 
                        ItemsSource="{Binding Documents}" 
                        SelectedItem="{Binding SelectedDocument}">
                <TabControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding FileName}" />

                            <TextBlock Text="*" Foreground="Red" FontWeight="Bold" Margin="2,0,0,0">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsDirty}" Value="True">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </StackPanel>
                    </DataTemplate>
                </TabControl.ItemTemplate>

                <TabControl.ContentTemplate>
                    <DataTemplate>
                        <TextBox Text="{Binding Content, UpdateSourceTrigger=PropertyChanged}" 
                                 AcceptsReturn="True" 
                                 VerticalScrollBarVisibility="Auto" 
                                 HorizontalScrollBarVisibility="Auto"
                                 FontFamily="Consolas" FontSize="14"/>
                    </DataTemplate>
                </TabControl.ContentTemplate>
            </TabControl>
        </Grid>
    </Grid>
</Window>